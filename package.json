{
  "name": "commit-reminder",
  "version": "1.0.0",
  "description": "check github and leetcode daily for commit, remind if missing, reward if not",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "nodemailer": "^8.0.5"
  }
}
